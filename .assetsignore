.git
.github
.gitignore
.assetsignore
wrangler.toml
README.md
# wrangler-action installs wrangler into the checkout at deploy time;
# without these the 122 MiB workerd binary fails the 25 MiB asset limit
node_modules
package.json
package-lock.json
.wrangler

# Worker source, schema and tests — not site content, served by the fetch
# handler / applied via wrangler, never meant to be a static file route.
worker
migrations
tests
vitest.config.js
